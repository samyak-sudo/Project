/**
 * Action type for setting search name
 * @constant {string}
 */
export const SET_SEARCH_NAME = 'SET_SEARCH_NAME';

// Create and freeze the exports object
const searchTypes = Object.freeze({
  SET_SEARCH_NAME
});

// Export default for compatibility
export default searchTypes;